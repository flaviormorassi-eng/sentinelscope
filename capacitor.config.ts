import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinelscope.app',
  appName: 'SentinelScope',
  webDir: 'dist/public'
};

export default config;
